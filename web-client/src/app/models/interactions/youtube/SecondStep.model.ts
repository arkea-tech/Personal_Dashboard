export class SecondStep
{
    channel_name: string;
    filter: string;

    constructor(channel_name: string, filter: string)
    {
        this.channel_name = channel_name;
        this.filter = filter;
    }
}
